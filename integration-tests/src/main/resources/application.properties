# A app de integração injeta o RedisGrpcClient; o producer exige host (mesmo sem
# conexão real — o GrpcClient do Vert.x só conecta na primeira chamada). Plaintext,
# sem auth: nada é contatado durante o teste.
quarkus.redis-grpc-client.host=localhost
